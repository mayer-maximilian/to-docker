deployment:
  apiContainer:
    image:
      repository: lucassctestregistry.azurecr.io/todo-api-image
      tag: v1
    resources:
      limits:
        cpu: 800m
        memory: 3000Mi
      requests:
        cpu: 650m
        memory: 2000Mi
  replicas: 1
kubernetesClusterDomain: cluster.local
service:
  ports:
  - port: 5008
    targetPort: 5008
  type: ClusterIP
